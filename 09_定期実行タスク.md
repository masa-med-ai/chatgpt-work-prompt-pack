# レベル9 — Scheduled taskを作る（Cloudとlocal）

**ねらい**: 同じ定期処理でも、Cloudとローカルプロジェクトでは動く条件が違うことを理解し、安全なレポート作成を先に試走してからScheduled taskにする。

**所要時間**: 15〜20分
**完成物**: ローカル実行では `workshop_output/level09/` の試走レポート。Cloud実行ではScheduledの実行結果と設定メモ。

## まず理解する違い

| 実行場所 | 向く仕事 | 動く条件 | 扱えるデータ |
|---|---|---|---|
| **Cloud** | 公開情報の調査、接続済みPluginを使う定期レポート | Cloudが選べれば、アプリを閉じたりPCを止めたりしても継続できる | アップロード済み資料、接続済みPlugin、チャットの文脈。ローカルフォルダには直接アクセスできない |
| **local project** | このPC内のファイルを読み、ローカル成果物を作る処理 | PCの電源が入り、ChatGPTデスクトップアプリが起動し、対象プロジェクトが利用可能であること | プロジェクト内のファイル。Gitリポジトリではローカル本体または分離したworktreeを選べる |

Cloudが表示されない場合はCloudを使える前提で進めない。ローカル実行は、PCがスリープ・終了している間に実行されるものではない。

## 安全と前提

- 最初のScheduled taskは、読む・要約する・レポートを作るだけにする。メール送信、予定作成、公開、削除、購入を定期実行に含めない。
- Scheduled taskは通常は無人で走るため、必要最小限の権限と対象だけを指定する。
- 定期化する**前に必ず通常のチャットで1回試走**し、出力、利用ツール、保存先を確認する。

## ChatGPTへの進行指示

開始時に、できること・全5ステップ・各ステップの1行要約を示し、「最初のステップを始めますか？」と確認する。以後は1ステップずつ進め、各ステップの最後に「次のステップに進みますか？」と尋ねる。

### Step 1. 定期化する小さなレポートを選ぶ

目的を一言で説明する: 「失敗しても影響のない、短く確認しやすい処理から始めます」。

次から1つ選んでもらう。

- **A. 今日のローカル作業メモ** — 指定フォルダの更新を3項目にまとめる（local向け）。
- **B. 公開情報の短いブリーフィング** — 指定テーマの新着をリンク付きでまとめる（Cloud向け）。

実行頻度は初回は平日1回など低頻度にし、出力の長さ、対象期間、止める条件を決める。

### Step 2. Cloudかlocal projectかを選ぶ

目的を一言で説明する: 「必要なデータがどこにあるかで、実行場所を決めます」。

次の質問で選ぶ。

1. ローカルフォルダのファイルが必要か。必要なら **local project** を選ぶ。
2. PCを閉じている間にも走らせたいか。必要なら、Cloudが利用可能な場合に **Cloud** を選ぶ。
3. Gitプロジェクトでファイルを作る可能性があるか。ある場合は、未完了の作業を守るためworktreeを優先する。

選択結果を「実行場所 / 使う資料・Plugin / 出力先 / PC・アプリの条件」の4点で短く確認する。Cloudではローカルの `workshop_output/` を保存先に指定できないため、チャットまたはScheduledの実行結果に残す。

### Step 3. まず通常のチャットで試走する

目的を一言で説明する: 「定期実行ではなく、今の1回で期待どおりの結果になるかを確かめます」。

次の要素を含む短い依頼文を作り、その場で1回だけ実行する。

```text
<対象>を確認し、<期間>の情報を<形式>で最大<件数>件にまとめてください。
出典または対象ファイルを示し、該当がなければ「該当なし」と書いてください。
外部への書き込みはせず、<出力先>だけに結果を残してください。
```

ローカル実行なら `workshop_output/level09/試走_YYYY-MM-DD.md` に保存する。Cloudなら、出力をチャット上で確認する。内容が広すぎる、ソースが不足する、不要な書き込みを提案する場合は、定期化せず依頼文を直して再試走する。

### Step 4. Scheduled taskとして登録する

目的を一言で説明する: 「試走済みの依頼文だけを、実行条件が分かる形で予約します」。

Scheduledを開き、次を参加者と確認してから登録する。

| 項目 | 確認する内容 |
|---|---|
| 実行場所 | Cloud / local project |
| 時刻と頻度 | 例: 平日 8:30。初回は低頻度にする |
| 実行単位 | 毎回独立した結果にするか、同じチャットの文脈を使うか |
| 入力 | 対象フォルダ、アップロード資料、接続済みPlugin |
| 出力 | Cloudの実行結果、または `workshop_output/level09/` |
| 禁止事項 | 送信、予定変更、公開、削除をしない |

登録直前に、保存する依頼文、実行場所、時刻・頻度、参照するファイル／Plugin、出力先、権限、停止条件を再掲する。「この内容でScheduled taskを作成してよいですか？」と尋ね、明示的な承認を得るまで作成しない。

ローカルGitプロジェクトでは、主作業中のファイルを変更しないレポートであっても、必要に応じてworktreeを選ぶ。Scheduled taskは設定時のサンドボックスと権限の範囲で動くため、広い権限を付け足さない。

### Step 5. 最初の実行結果を確認する

目的を一言で説明する: 「予約して終わりにせず、最初の出力を見て頻度と依頼文を調整します」。

Scheduledで状態と実行結果を確認する。次の3点を満たさない場合は、停止または調整してから次回を待つ。

1. 指定した対象だけを扱っている。
2. 出力が短く、確認しやすい。
3. 外部への書き込みや想定外のファイル変更がない。

ローカル実行が予定時刻に動かなかった場合は、PC、アプリ、プロジェクトの利用可能性を確認する。Cloud実行が利用できない場合は、Cloud前提のスケジュールを作らず、ローカルの条件を満たせる時間帯に変更する。

最後は成果物と学びを3行以内でまとめ、`AGENTS.md` の完了手順に従う。
