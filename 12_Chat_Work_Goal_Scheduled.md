# レベル12 — Chat／Work／Goal／Scheduledを使い分ける

**ねらい**: 同じ依頼でも、答えを得るだけなのか、成果物を作るのか、完了まで進めるのか、定期化するのかで、使う実行形態を選べるようにする。

**所要時間**: 15〜20分
**完成物**: `workshop_output/level12/使い分けメモ.md`

## 最初に伝える早見表

| 実行形態 | 向く依頼 | 終わり方 | 選ばないほうがよい場面 |
|---|---|---|---|
| **Chat** | 質問、相談、短い下書き、意思決定の補助 | その場で回答を確認して終える | 多数の資料・ファイルを使う成果物作り |
| **ChatGPT Work** | 複数ステップ、資料やPluginを使う作業、レビュー可能な成果物 | 成果物と確認結果をレビューして終える | 一言で答えられる質問だけ |
| **Goal** | デスクトップアプリのローカル作業で、明確な完了条件がある長めの作業 | `/goal`で結果・制約・検証条件を満たしたら完了 | HostedなCloud Workや、定期的に同じ処理を走らせたいとき |
| **Scheduled task** | 同じ確認・要約・監視を時刻または間隔で繰り返す仕事 | 最初の実行結果をレビューし、継続的に調整する | 試走していない処理、書き込みを伴う危険な自動化 |

覚え方は次の順である。

1. まず答えや相談が欲しい → **Chat**
2. まとめて成果物まで作りたい → **ChatGPT Work**
3. 完了条件を決め、途中で進捗を見ながら終わらせたい → **Goal**
4. 同じ仕事を将来も繰り返したい → **Scheduled task**

## 安全と前提

- Goalはデスクトップアプリのローカル作業などで使う機能であり、権限を広げたり、定期実行に変えたりはしない。HostedなCloud Workでは`/goal`を使わず、結果・制約・検証条件を通常の依頼文へ含める。
- Scheduled taskは無人で動くことがある。初回は読む・要約するだけにし、メール送信、予定作成、公開、削除、購入を含めない。
- Scheduled taskを作る前に、同じ依頼を通常のChatまたはWorkで**必ず1回試走**する。
- ローカルのファイルが必要な作業はWork locally、PCを閉じても続けたい公開情報中心の作業はCloudが利用できる場合に選ぶ。Cloudはローカルフォルダを直接扱えない。

## ChatGPTへの進行指示

開始時に、できること・全5ステップ・各ステップの1行要約を示し、「最初のステップを始めますか？」と確認する。以後は1ステップずつ進め、各ステップの最後に「次のステップに進みますか？」と尋ねる。

### Step 1. 依頼を4つの質問で分類する

目的を一言で説明する: 「道具の名前からではなく、必要な結果から選びます」。

参加者が普段頼みたいことを1つ選ぶ。例として「毎週の研究メモを整理したい」を使ってよい。次の4問に順に答え、使い分けの候補を決める。

1. その場の答えだけでよいか。
2. ファイル、複数資料、Pluginを使う成果物が必要か。
3. 終了を判定できる条件があるか。
4. 同じ処理を将来も繰り返したいか。

回答と選んだ実行形態を、`workshop_output/level12/使い分けメモ.md` の最初に記録する。

### Step 2. 同じ題材を4通りの依頼文にする

目的を一言で説明する: 「依頼文の違いを見ると、各実行形態の役割が分かります」。

選んだ題材を、次のテンプレートで4通りに書き換える。

| 実行形態 | 依頼文の型 |
|---|---|
| Chat | 「<テーマ>について、判断に必要な要点を3点で教えてください」 |
| ChatGPT Work | 「<資料・対象>を使い、<成果物>を作ってください。保存先、制約、レビュー条件は<内容>です」 |
| Goal（desktop local） | 「`/goal` <完成した結果>。制約: <守ること>。完了条件: <確認方法>」 |
| Scheduled task | 「<頻度>に<対象>を確認し、<形式>で報告してください。<停止条件>。外部への書き込みはしないでください」 |

Goalが利用できない場合は、Goal用の依頼文をメモに残すだけにする。Scheduled用の依頼文は、まだ登録しない。

### Step 3. ChatとWorkの境目を体験する

目的を一言で説明する: 「短い相談はChat、材料を集めて成果物にするならWorkです」。

まずChatで、選んだ題材について短い要点を得る。次に、Work locallyが利用できる場合は、その要点を材料にして `workshop_output/level12/` に1ページのメモまたはチェックリストを作る。

Workには、次の4点を含めて依頼する。

- 使う資料または参照範囲
- 保存先
- 変えてはいけないもの
- 完成後に確認する条件

Cloudを選べる場合は、ローカルファイルが不要な公開情報の調査だけをCloudで試し、Cloudの結果とローカル成果物を混同しない。

### Step 4. Goalで途中の進路修正を体験する

目的を一言で説明する: 「長い作業は、完了条件を示してから途中で条件を足します」。

Work locallyのチャットでGoalが利用できる場合は、Step 2で作ったGoal用の依頼文を開始する。HostedなCloud Workでは`/goal`を入力せず、同じ結果・制約・完了条件を通常の依頼文として送る。進捗を確認した後、次のように制約を1つ追加する。

```text
成果物はA4 1ページ相当の分量に収め、変更したファイルと確認結果を最後に一覧で示してください。
```

Goalの進捗表示から、一時停止・再開・編集ができることを確認する。作業の目的が毎週同じ報告を作ることに変わった場合は、Goalを続けるのではなく、試走後にScheduled taskを検討する。

### Step 5. 試走済みの処理だけをScheduled taskにする

目的を一言で説明する: 「定期化する前に、毎回同じ品質で読める出力かを確かめます」。

Step 2のScheduled用依頼文を、通常のChatまたはWorkで1回試走する。次を満たした場合だけScheduledに登録する。

1. 対象、期間、出力形式、停止条件が具体的である。
2. 出力を参加者が短時間で確認できる。
3. 外部サービスへの書き込みや不要なファイル変更がない。

登録時は、Cloudかlocal projectか、時刻・頻度、同じチャットの文脈を使うか、出力先を確認する。保存する依頼文、参照するファイル／Plugin、権限、停止条件も再掲し、「この内容でScheduled taskを作成してよいですか？」と尋ねる。明示的な承認を得るまで登録しない。

local projectではPC、ChatGPTデスクトップアプリ、対象プロジェクトが実行時に利用できる必要がある。Cloudではローカルの `workshop_output/` を直接扱えない。

`使い分けメモ.md` の最後に「選んだ実行形態 / 選んだ理由 / 試走結果 / 次に見直す条件」を追記する。最後は成果物と学びを3行以内でまとめ、`AGENTS.md` の完了手順に従う。
