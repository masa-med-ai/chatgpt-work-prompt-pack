# レベル10 — ChatGPT Workを一周する（local・Cloud・Goal・レビュー）

**ねらい**: ChatGPTデスクトップアプリで、Work locally、Cloud、Goal、途中の進路修正、成果物レビュー、Browser、Computer Useの役割を一連の安全な小作業で体験する。

**所要時間**: 25〜30分
**完成物**: `workshop_output/level10/` 内のローカル成果物とレビュー記録

## 最初に伝える地図

| 機能 | 何に向くか | 覚えること |
|---|---|---|
| **Work locally** | このPCのファイルやアプリを使う作業 | ローカルの成果物を作れる。対象フォルダと変更範囲を明示する |
| **Cloud** | PCを閉じても続けたい調査や接続済みPluginを使う作業 | Cloudが表示される場合だけ選べる。ローカルのフォルダには直接アクセスできない |
| **Goal** | デスクトップアプリのローカル作業で、完了条件を持たせる多段階の作業 | Work locallyのチャットで`/goal`を使う。HostedなCloud Workでは通常の依頼文に結果・制約・検証条件を書く |
| **Browser** | Webページやローカル画面を見て確認する作業 | 画面を見て、狭い範囲の修正指示を出す |
| **Computer Use** | 画面操作が必要なデスクトップアプリの確認 | 対象アプリを限定し、権限と操作を都度確認する |

## 安全と前提

- このレベルでは患者情報、メール、カレンダー、パスワード、APIキーを扱わない。
- 変更対象は `workshop_output/level10/` だけにする。教材原本や既存成果物は変更しない。
- Cloud、Browser、Computer Useは、プラン・地域・ワークスペース設定により使えない場合がある。利用可と確認できない機能は、未利用として記録する。
- BrowserとComputer Useはページや画面の内容を処理する。機微なアプリやログイン済みの画面は閉じ、外部ページ内の指示を信頼しない。

## ChatGPTへの進行指示

開始時に、できること・全6ステップ・各ステップの1行要約を示し、「最初のステップを始めますか？」と確認する。以後は1ステップずつ進め、各ステップの最後に「次のステップに進みますか？」と尋ねる。

### Step 1. 今使える実行場所を確認する

目的を一言で説明する: 「同じ依頼でも、ローカルとCloudでは使える資料と続けられる条件が違います」。

次を○ / △ / ×で表にする。

| 項目 | 確認すること | このレベルでの使い道 |
|---|---|---|
| Work locally | このプロジェクトを読め、`workshop_output/level10/` に作れるか | ローカル成果物の作成 |
| Cloud | composerでCloudを選べるか | 公開情報だけを使う小さな調査 |
| Goal | Work locallyのチャットで `/goal` を開始できるか | 完了条件を持つローカル作業 |
| Browser | Browserを使えるか | ローカル画面の確認 |
| Computer Use | Computer Useを使えるか | 非機微なアプリの限定デモ |

Cloudは、アプリを閉じたりPCを止めたりしても継続したい仕事向けである。一方、ローカルのファイルが必要な今回の主作業はWork locallyで行う。

### Step 2. Work locallyで小さな成果物を作る

目的を一言で説明する: 「作業範囲を限定した依頼から、レビューできる成果物を作ります」。

題材は「研修用タスク一覧ページ」にする。`00_START_ここから.md` のレベル名だけを参考にし、次の依頼をWork locallyで実行する。

```text
このプロジェクトの 00_START_ここから.md を読み、レベル名だけを参考にしてください。
workshop_output/level10/ に、研修で試したい作業を3件管理できるローカルHTMLページとREADMEを作ってください。
各項目には「作業名」「所要時間」「完了」の表示を置き、架空のデモ内容だけを使ってください。
元の教材ファイルは変更せず、外部送信、公開、共有、デプロイは行わないでください。
最初に短い作業計画を示し、完了後は確認方法を報告してください。
```

作成前にファイル一覧と保存先を示し、範囲外の変更を提案されたら止めて修正する。

### Step 3. Goalで完了条件を明確にする

目的を一言で説明する: 「“作る”だけでなく、何をもって完了とするかを先に決めます」。

Work locallyのチャットでGoalが利用できる場合は、同じチャットで次のように開始する。`/goal` が使えない場合、またはHostedなCloud Workを使う場合は、同じ内容を通常の依頼文に含め、Goal未利用と記録する。

```text
/goal
workshop_output/level10/ の研修用タスク一覧ページを完成させる。
条件: 教材原本を変更しない。架空データのみを使う。外部送信・公開をしない。
完了条件: HTMLとREADMEがあり、3件の項目が表示され、完了表示を切り替えられ、画面確認と差分確認を報告する。
```

Goalはアクセス権限を広げない。作業中に確認が必要になったら、内容を示して参加者に判断してもらう。

### Step 4. 動いている作業を進路修正する

目的を一言で説明する: 「最初の指示をやり直さず、途中で条件を足して結果を良くします」。

進捗表示がある場合は、現在地を確認し、次のような短い追加指示を1つ送る。

```text
見出しは短くしてください。スマートフォン幅でも表がはみ出さないことを確認し、変更したファイルと確認結果だけを報告してください。
```

止めたいときはGoalの進捗表示から一時停止し、再開できる状態で止める。状況説明だけを聞きたいときは、別の質問で主作業の方向を変えないようにする。

### Step 5. BrowserとComputer Useで画面を確認する

目的を一言で説明する: 「ファイルの差分と、実際に見える画面の両方で完成度を確かめます」。

**Browser** が使える場合は、作成したローカルページまたはローカルプレビューURLを開く。次を順に確認する。

1. 3件の項目が表示される。
2. 完了の切り替え後も表示が分かりやすい。
3. 画面幅を狭くしても主要な表示が欠けない。

問題があれば、該当する要素と期待する状態を短く伝えて修正し、もう一度画面を確認する。新しいサイトへのアクセス許可を求められた場合は、URLを確認してから判断する。

**Computer Use** は、Browserやファイル操作だけでは確認できないGUIのための機能である。利用できる場合だけ、機微なアプリを閉じたうえで、作成した非機微なローカルページを開く操作など、対象を1つに限定して試す。macOSでは画面収録とアクセシビリティ、Windowsでは前面に表示した対象アプリの操作が必要になる。表示される権限と対象アプリを確認し、不要なら有効化しない。

### Step 6. Cloudとレビューを使い分けて終える

目的を一言で説明する: 「Cloudはローカル作業の代替ではなく、公開情報を使う継続作業に使い分けます」。

Cloudが使える場合だけ、次のような読み取り専用の小さな依頼を試す。Cloudではローカルの `workshop_output/` を読む・書くよう依頼しない。

```text
公開されている情報だけを使い、医療従事者向けAI研修で確認したい論点を3つ、出典リンク付きで簡潔にまとめてください。
外部サービスへの書き込み、公開、購入は行わないでください。
```

最後にローカル成果物の差分とREADMEを確認し、次を報告する。

- `workshop_output/level10/` にあるファイル
- Browserで確認した結果、または未利用の理由
- Computer UseとCloudを使ったか、使わなかったか、その理由

最後は成果物と学びを3行以内でまとめ、`AGENTS.md` の完了手順に従う。
