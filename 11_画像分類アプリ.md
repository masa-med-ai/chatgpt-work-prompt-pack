# レベル11 — 画像を理解し、ローカルAIアプリを作る（公開digits）

**ねらい**: Image inputで画像を説明してもらう体験と、公開の手書き数字データで学習・評価・予測するローカルAIアプリ作りを分けて学ぶ。

**所要時間**: 25〜30分
**完成物**: `workshop_output/level11/digits-app/` の学習コード、評価結果、ローカルアプリ、README

## 最初に必ず伝える安全・倫理の注意

- 使うのは `scikit-learn` の公開教材データ `load_digits` に含まれる手書き数字だけである。実患者画像、院内画像、顔写真、検査画像、スクリーンショットを入力・学習・保存しない。
- この演習の予測は0〜9の数字分類の教育デモであり、診断、トリアージ、臨床判断、医療機器の評価には使えない。
- Image inputは、ChatGPTに画像を説明・比較してもらうための入力である。アプリの学習データや臨床的な根拠と混同しない。
- 画像・モデル・結果はローカルの `workshop_output/level11/` に置き、公開、共有、外部送信はしない。

## ChatGPTへの進行指示

開始時に、できること・全6ステップ・各ステップの1行要約を示し、「最初のステップを始めますか？」と確認する。以後は1ステップずつ進め、各ステップの最後に「次のステップに進みますか？」と尋ねる。

### Step 1. 公開digitsデータを見て、Image inputを試す

目的を一言で説明する: 「AIに渡す画像と、AIを学習させるデータは別のものだと理解します」。

Work locallyで、公開digitsデータから0〜9の見本を並べた画像を `workshop_output/level11/digits-app/sample_digits.png` に作る。画像をチャットに添付し、次のように依頼する。

```text
この画像は公開の手書き数字データです。左から右、上から下の順に見える特徴を短く説明し、数字の形が似ていて区別が難しそうな組み合わせを挙げてください。
医療的な推論はせず、画像に見える特徴だけを扱ってください。
```

説明が画像と合っているか参加者が確認する。この会話は画像理解の練習であり、ここで得た文章を学習ラベルとして使わない。

### Step 2. ローカルアプリの仕様を決める

目的を一言で説明する: 「小さくても、入力・予測・根拠表示がそろったアプリにします」。

次の仕様を確認する。

| 項目 | 今回の内容 |
|---|---|
| データ | `sklearn.datasets.load_digits` の公開データのみ |
| 入力 | テスト用の手書き数字見本を選ぶ |
| 出力 | 予測数字、正解ラベル、確信度の代わりの近傍情報またはスコア |
| 評価 | 学習用とテスト用を分けた正解率、混同行列 |
| 保存先 | `workshop_output/level11/digits-app/` |

自由描画や任意画像のアップロードは発展機能であり、初回は含めない。入力の前処理が変わると精度の意味も変わるため、既知のテスト見本で正しく動くことを先に確かめる。

### Step 3. 学習・評価コードを作る

目的を一言で説明する: 「学習結果を再現できる形で保存し、数字だけで判断しないようにします」。

Work locallyで、次の依頼を実行する。

```text
workshop_output/level11/digits-app/ に、公開の sklearn.datasets.load_digits だけを使う数字分類デモを作ってください。
train_and_evaluate.py で固定した乱数seedの学習・テスト分割、K近傍法による学習、正解率、混同行列、見本画像の出力を行ってください。
ローカルアプリ用のモデルと、README、requirements.txt を作ってください。
実患者画像や外部データを使わず、ネットワーク送信、公開、共有はしないでください。
既存教材は変更せず、最初に実行方法を報告してください。
```

実行前に、Pythonと必要なパッケージが利用できるかを確認する。追加インストールが必要な場合は、パッケージ名、入手先、保存先、必要性を示し、参加者の承認を得てから行う。ダウンロード可能な別データセットを代わりに使わない。

### Step 4. 評価結果を読む

目的を一言で説明する: 「正解率だけではなく、どの数字どうしを間違えたかを見ます」。

学習・評価を1回実行し、次を短く示す。

| 確認項目 | 報告する内容 |
|---|---|
| データ分割 | 学習用とテスト用の件数、固定seed |
| 正解率 | 実測値。期待値や臨床性能とは扱わない |
| 混同行列 | 間違いが多い組み合わせを1〜2点 |
| 再現方法 | 実行したコマンドまたは操作 |

エラーや低い結果が出た場合は、データ分割、前処理、実行ログを確認する。都合の良い数値に書き換えたり、検証用データを学習に混ぜたりしない。

### Step 5. ローカルアプリとして動かす

目的を一言で説明する: 「学習済みモデルを、参加者が操作して確認できる画面にします」。

既存環境に対応する最小のローカル画面を作る。たとえばStreamlitが利用可能なら、テスト見本を選択して予測と正解を表示する `app.py` にする。利用できない場合は、追加インストールを勝手に行わず、既存環境で動くローカル画面または操作手順を提案し、参加者が選ぶ。

アプリは次を満たすようにする。

1. 予測対象が公開digitsのテスト見本であると画面に明記する。
2. 予測数字と正解ラベルを並べて表示する。
3. 「教育デモであり、臨床利用不可」と表示する。

Browserが使える場合はローカルURLを開き、異なる3見本で予測・正解・表示崩れを確認する。Browserが使えない場合は、参加者がローカル画面を確認し、未検証の点をREADMEに記す。

### Step 6. 変更範囲と限界をレビューする

目的を一言で説明する: 「動くことと、安全に使える範囲を分けて確認します」。

最後に次を確認する。

- `workshop_output/level11/digits-app/` にコード、評価結果、READMEがある。
- 教材原本、実患者画像、外部サービスを扱っていない。
- READMEにセットアップ、実行、確認した3見本、教育デモの限界が書かれている。
- 予測結果を医療判断に利用しないことが画面とREADMEの両方にある。

任意の発展では、公開ライセンスと用途が確認できる別の非医療教材データを使う。実患者画像を使う発展課題は、このワークショップの範囲外である。

最後は成果物と学びを3行以内でまとめ、`AGENTS.md` の完了手順に従う。
