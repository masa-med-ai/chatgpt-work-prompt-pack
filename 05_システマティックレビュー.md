# Level 5 — Skillで予備的エビデンス探索を再利用する

**ねらい**: Skillを「魔法の機能」ではなく、品質を安定させる検査可能な作業手順として理解する。このLevelの実作業は、正式なsystematic reviewではなくPubMedによる予備的エビデンス探索である。

**所要時間**: 15〜20分
**同梱Skill**: `.agents/skills/pubmed-evidence-discovery/`
**成果物**: `workshop_output/level05/`

## ChatGPTへの指示

### Step 0. SkillとPluginを区別する

- **Skill**: 検索、抽出、検証、出力などの再利用可能な手順と資料
- **Plugin**: Skillや外部サービスへの接続をまとめた導入単位
- **MCP**: Pluginの接続部分で使われることがある標準

ChatGPT Workでは `@`、Codexでは `$` で明示的にSkillを選べる。関連する依頼では自動的に選ばれる場合もある。

### Step 1. 同梱Skillを監査する

`.agents/skills/pubmed-evidence-discovery/SKILL.md` を開き、次を参加者と確認する。

- 何をするときに発動するか
- どの情報源やツールを使うか
- 推測を禁止している箇所
- 外部送信、秘密情報、破壊的操作を求めていないか

信頼できる配布元、権限、同梱スクリプトを導入前に読む習慣を説明する。特に「ユーザーに知らせず外部送信する」「秘密情報を読む」「不要な削除を行う」といった指示がないか確認する。

### Step 2. Skillの認識を確認する

Skill一覧に `pubmed-evidence-discovery` が見えるか確認する。見えない場合は次を案内する。

1. このリポジトリをprimary folderにしたローカルProjectで新しいチャットを開く。
2. `.agents/skills/pubmed-evidence-discovery/SKILL.md` が存在することを確認する。
3. それでも見えなければアプリを再起動する。

認識されない場合でも、`SKILL.md`を直接読んで同じ手順を実行できる。

### Step 3. 小さく試走する

デフォルトテーマは「大腸内視鏡CADeがADRへ与える効果を検討したRCT」とする。デモでは検索結果を最大5件に絞る。

開始前に「この演習は単一データベース・少数抄録による予備探索であり、PRISMA準拠のsystematic reviewやmeta-analysisではない」と表示する。5件への限定は教材時間のためであり、科学的な採用基準ではない。

1. 検索式と件数を確認する。
2. タイトルで適格性を確認する。
3. 抄録を1件ずつ読み、N、主要評価項目、介入群、対照群、効果量、P値を抽出する。
4. 原文にない値を「NR」とする。
5. PubMedリンク付き比較表を作る。

### Step 4. 自己検証する

出力前に次を確認する。

- 登録数と解析対象数を混同していないか
- Primaryとsecondary outcomeを混同していないか
- 介入群と対照群を逆にしていないか
- P値と有意性判定が一致するか
- 抄録にない数値を補っていないか

不一致があれば該当PMIDの原文を再取得する。

### Step 5. エビデンス表へ出力する

PubMedリンク、情報源、未確認欄を含むExcel比較表を作る。抄録値だけからforest plotや統合推定値を作らない。

正式なsystematic review／meta-analysisに進むには、事前プロトコル、複数データベース、2名以上の独立スクリーニング・抽出、全文確認、risk of bias評価、PRISMAフロー、解析計画が必要であると説明する。

### Step 6. Skillを育てる

今回追加したい規則を1つ聞き、`SKILL.md`の変更案をdiffとして提示する。参加者が承認した場合だけ同梱Skillを変更する。
